import { createFileRoute, Link } from "@tanstack/react-router";
import { canonicalUrl } from "../../siteConfig";
export const Route=createFileRoute("/resources/document-scanner-vs-camera")({head:()=>({meta:[{title:"Document Scanner App vs Phone Camera: What's the Difference? — DocSnap"},{name:"description",content:"Compare a normal phone photo with a document scanner workflow using cropping, perspective correction, OCR, multi-page PDFs, and document-focused image processing."},{property:"og:type",content:"article"}],links:[{rel:"canonical",href:canonicalUrl("/resources/document-scanner-vs-camera")}]}),component:Guide});
function Guide(){return <main className="min-h-screen bg-gray-950 px-6 py-14 text-white"><article className="mx-auto max-w-3xl"><Link to="/resources" className="text-sm text-indigo-400">← Resources</Link><p className="mt-8 text-sm font-semibold uppercase tracking-wider text-indigo-400">Scanner vs camera</p><h1 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">Document scanner app vs. your phone camera</h1><p className="mt-6 text-lg leading-8 text-gray-300">Both start with the same camera sensor. The difference is what happens to the image after you press the shutter.</p><section className="mt-10 space-y-5 leading-8 text-gray-300"><h2 className="text-2xl font-semibold text-white">A normal camera photo keeps the whole scene</h2><p>A quick photo is fine when you only need a visual reference. It commonly includes the background and remains an image rather than a document-oriented file.</p><h2 className="text-2xl font-semibold text-white">A scanner workflow isolates the document</h2><p>Document edge detection, cropping, deskewing, perspective correction, and document-focused image adjustments can turn the camera capture into a cleaner page.</p><h2 className="text-2xl font-semibold text-white">Scanner apps can keep pages together</h2><p>Instead of storing each page as a separate photo, a scanning workflow can combine and reorder pages into one PDF.</p><h2 className="text-2xl font-semibold text-white">OCR adds something the camera roll does not</h2><p>OCR recognizes words in the scan. A searchable PDF can preserve the page image while adding a text layer that makes its contents easier to find later.</p><h2 className="text-2xl font-semibold text-white">When is a regular photo enough?</h2><p>Use the camera when you simply need to remember what something looks like. Use a scanner workflow when the item is paperwork you intend to save, search, share, print, or organize as a document.</p></section><div className="mt-12 flex flex-wrap gap-3"><Link to="/scan" className="rounded-lg bg-indigo-600 px-5 py-3 font-semibold">Try DocSnap free</Link><Link to="/resources/searchable-pdf-ocr" className="rounded-lg border border-gray-700 px-5 py-3 font-semibold">Searchable PDFs →</Link></div></article></main>;}